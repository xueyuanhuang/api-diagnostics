import type { Metadata } from 'next';
export const metadata: Metadata = {
  title: 'API Tests: Token Usage, Endpoints and RPM | API Diagnostics',
  description: 'Check token usage, compare API endpoints, inspect tool responses and test request-rate capacity with your own provider connection.',
  alternates: { canonical: '/tests' },
};
export default function TestsPage() { return null; }
