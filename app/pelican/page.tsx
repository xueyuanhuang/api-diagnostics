import type { Metadata } from 'next';
export const metadata: Metadata = {
  title: 'Run the Pelican SVG Animation Test | API Diagnostics',
  description: 'Test your AI model with a pelican riding a bicycle animation prompt. Preview generated HTML and SVG, download the result, or sign in to save runs.',
  alternates: { canonical: '/pelican' },
};
export default function PelicanPage() { return null; }
