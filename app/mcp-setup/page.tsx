export default function McpSetup() {
  const base='https://api-diagnostics.xue-yuanhuang.workers.dev';
  return <main className="mx-auto max-w-3xl space-y-6 p-8"><h1 className="text-3xl font-semibold">Review your tests in ChatGPT</h1><p>Add these two read-only MCP connections in ChatGPT’s Plugins page with Developer mode enabled.</p><section className="space-y-3 rounded-xl border p-5"><h2 className="text-xl font-semibold">API Diagnostics Review</h2><code className="break-all">{base}/mcp</code><p>Authentication: OAuth, using dynamic client registration. Sign in to this website with Google, then allow read-only access. Reads your saved tests and public source code. Cannot access API keys, run tests, edit or delete anything. Access lasts seven days; reconnect after it expires.</p></section><section className="space-y-3 rounded-xl border p-5"><h2 className="text-xl font-semibold">BazaarLink Public Research</h2><code className="break-all">{base}/mcp/bazaarlink</code><p>Authentication: None. An unofficial adapter reading BazaarLink’s public reports, baselines, guide, and open-source engine. No keys are sent to BazaarLink and no tests are started.</p></section><section className="space-y-3"><h2 className="text-xl font-semibold">Review prompt</h2><p>Select GPT-6 Pro if it is available with these tools in your account, and attach both connections.</p><blockquote className="border-l-4 pl-4">Review API Diagnostics and BazaarLink using both MCP connections. Inspect our methods, representative saved results, source code and tests, then BazaarLink’s public baselines, reports and engine. Separate verified findings from provider claims; examine false positives, abstention, token accounting, latency, privacy, licensing, and cost. Recommend what to reuse, independently implement, or avoid. Produce a prioritized implementation plan with file-level changes and acceptance tests. Do not run paid tests or change data.</blockquote></section><form action="/oauth/disconnect" method="post"><button className="rounded-lg border p-3">Revoke all ChatGPT access to my results</button></form><a href="/tests" className="underline">Back to tests</a></main>;
}
